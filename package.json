{
  "scripts": {
    "server": "node server/index.js",
    "client": "npm start --prefix vertex-ai",
	"dev": "concurrently \"npm run server\" \"npm run vertex-ai\"",
	"build": "npm install && npm install --prefix server && npm install --prefix vertex-ai && npm run build --prefix vertex-ai"
  },
  "dependencies": {
    "concurrently": "^9.1.2",
    "react-router-dom": "^7.1.3"
  }
}
